# Logfile for processes in FFMPEG through pydub (service/transcoder.py).
# Look here if something doesn't seem to work like intended, but doesn't throw exception.

Note: Tests will append to this file so make sure you don't mix up actual errors with test errors.
-----------------------------------------------------------
